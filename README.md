# Task6
To-do list
```
# Java Swing To-Do App

A simple desktop To-Do list application built with **Java Swing**.  
This app allows you to add and delete tasks using a graphical user interface (GUI).

---
** Features
- **Add Tasks** — Enter a task in the text field and click `Add` (or press `Enter`).
- **Delete Tasks** — Select a task and click `Delete Selected` (or press `Delete` key).
- **Scrollable Task List** — Automatically adds a scroll bar when tasks exceed visible area.
- **Keyboard Shortcuts** —  
  - `Enter` to add  
  - `Delete` to remove selected task
- **Confirmation Dialog** — Prevents accidental deletions.
- **Tooltips** — Guides new users on how to use the app.

---

**Technologies Used
- **Java SE (JDK 8+)**
- **Swing** — Built-in Java GUI toolkit
- **AWT Event Handling**


## 📂 Project Structure
